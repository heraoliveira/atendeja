server:
  forward-headers-strategy: framework

spring:
  jpa:
    properties:
      hibernate:
        format_sql: false

atendeja:
  auth:
    demo-users:
      enabled: ${DEMO_AUTH_USERS_ENABLED:false}
